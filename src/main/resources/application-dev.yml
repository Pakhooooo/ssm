server:
  port: 8080

spring:

  datasource:
    url: jdbc:mysql://192.168.1.99:3306/ssm?characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
  
  redis:
    host: 192.168.1.99
    port: 6379
    password: 
    timeout: 2000
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
        max-wait: 2000ms
          
          
          
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG

# 配置全局异常处理
#errors:
#  include-stacktrace: never
#  whitelabel:
#    enabled: false

# 配置 Swagger
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# 配置 JWT
jwt:
  secret: your_jwt_secret_key
  expiration: 86400 # 24小时过期时间（以秒为单位）

# 配置自定义错误页面
#server:
#  error:
#    whitelabel:
#      enabled: false
#    include-message: always
#    include-binding-errors: always

# 配置缓存
#cache:
#  type: redis
#  redis:
#    time-to-live: 60s
    
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.ssm
  configuration:
    map-underscore-to-camel-case: true