<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.user.mapper.UserInfoMapper">

    <select id="getUserByUserName" parameterType="java.lang.String" resultType="UserVO">
        SELECT 
            u.id AS userId, user_name AS userName, role_name AS role
        FROM t_user u
        JOIN t_user_role ur 
            ON u.id = ur.user_id AND ur.del_status = 0
        JOIN t_role r 
            ON ur.role_id = r.id AND r.del_status = 0
        WHERE u.del_status = 0 AND u.user_name = #{userName}
    </select>

</mapper>